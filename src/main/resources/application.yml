server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springuser
    username: root
    password: acer
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
